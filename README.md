# INSTRUCTIONS

# 1. Installation:

```
pip install modputs==0.0.2.
```
# 2. Examples:

```
from modputs import INPUT, READ
INPUT("",1, 1, NPRINT=True)
READ()
if READ.userinput == 'test':\n
    print('Test complete!')
```
